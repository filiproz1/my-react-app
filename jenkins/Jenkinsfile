pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Test') { 
            agent {
                docker {
                    image 'my-react-app:latest'
                }
            }
            steps {
                sh 'npm run test'
            }
        }

        stage('Deploy') { 
            agent {
                docker {
                    image 'my-react-app:latest'
                }
            }
            steps {
                sh 'npm run build'
                sh 'npx serve build'
            }
        }
    }
}